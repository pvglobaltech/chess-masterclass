DATABASE_URL="postgresql://chess_0vux_user:5AAjHR9EhUH3A547YBlVFQsuXwA4Bp2X@dpg-d9djccn7f7vs738pfsag-a/chess_0vux"
JWT_SECRET="replace-with-a-long-random-string"
PORT=4000
CLIENT_ORIGIN="http://localhost:3000"

# Module 1 — Stripe (test keys from https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."          # from `stripe listen` in dev, or the webhook endpoint in prod
STRIPE_SUCCESS_URL="http://localhost:3000/register/success"
STRIPE_CANCEL_URL="http://localhost:3000/register"

# Module 1 — receipt emails. Any SMTP account works (Gmail app password,
# Mailgun, Resend SMTP, etc). Leave blank in dev to just log emails to console.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="Chess MasterClass <no-reply@chessmasterclass.ca>"

# TODO(module-4): add SENDGRID_API_KEY / TWILIO creds for real SMS + bulk marketing email
